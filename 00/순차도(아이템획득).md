```mermaid
sequenceDiagram
    autonumber
    actor User as 플레이어
    participant UI as Add_Item_UI
    participant System as 전투
    participant P as 플레이어
    participant Char as 캐릭터
    participant Inv as 인벤토리
    
    User->>UI: 정보 입력(플레이어id, 아이템명, 타입, 가치)
    activate UI
    UI->>System: 아이템획득(플레이어id, 아이템명, 타입, 가치)
    activate System
    
    System->>P: 플레이어체크(플레이어id)
    activate P
    P-->>System: boolean
    deactivate P
    
    alt 검증 통과
        System->>Char: 인벤토리 접근
        System->>Inv: 아이템추가요청(아이템명, 타입, 가치)
        activate Inv
        
        alt 인벤토리 용량 < 10
            create participant Item as 아이템
            Inv->>Item: <<create>> 생성 및 등급부여(가치 판별)
            Inv->>Inv: 아이템리스트에 추가
            Inv-->>System: true (추가 성공)
            System-->>UI: 아이템 획득 완료 및 등급 출력
        else 인벤토리 가득 참 (>= 10)
            Inv-->>System: false (추가 실패)
            System-->>UI: 에러 메시지 (인벤토리가 가득 찼습니다)
        end
        deactivate Inv
    else 검증 실패
        System-->>UI: 에러 메시지 (플레이어 체크 실패)
    end
    deactivate System
    deactivate UI