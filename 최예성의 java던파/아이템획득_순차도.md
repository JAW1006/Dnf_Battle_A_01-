```mermaid

sequenceDiagram
    autonumber
    actor 사용자 as 플레이어
    participant UI as Add_Item_UI
    participant Control as 전투
    participant PlayerClass as 플레이어(클래스)
    participant Char as 캐릭터(현재캐릭터)
    participant Inv as 인벤토리
    
    사용자->>UI: 아이템 획득 요청 (플레이어id, 아이템명, 타입, 가치)
    activate UI
    
    UI->>Control: 아이템획득(플레이어id, 아이템명, 타입, 가치) : void
    activate Control
    
    %% <<include>> 플레이어 체크 수행
    Control->>PlayerClass: 플레이어체크(플레이어id) : boolean
    activate PlayerClass
    PlayerClass-->>Control: true (반환)
    deactivate PlayerClass
    
    %% 캐릭터에게 인벤토리 추가 작업 위임
    Control->>Char: 인벤토리멤버추가(아이템명, 타입, 가치) : void
    activate Char
    
    %% 아이템 객체 생성 및 자체 등급 판정 로직
    create participant Item as 아이템
    Char->>Item: <<create>> 생성(아이템명, 타입, 가치)
    activate Item
    Note over Item: 생성자 내부 로직:<br/>가치에 따라 등급(전설/희귀/일반) 자동 부여
    Item-->>Char: 아이템 객체 반환
    deactivate Item
    
    %% Composition 관계인 인벤토리에 추가
    Char->>Inv: 아이템추가(새아이템) : boolean
    activate Inv
    alt 인벤토리 용량(10) 미만
        Note over Inv: 아이템리스트에 추가 성공
        Inv-->>Char: true
    else 인벤토리 가득 참
        Note over Inv: 최대용량 초과로 추가 거부
        Inv-->>Char: false
    end
    deactivate Inv
    
    Char-->>Control: void
    deactivate Char
    
    Control-->>UI: void
    deactivate Control
    
    %% UI 단에서 인벤토리 수량 비교로 결과 판정
    Note over UI: UI에서 획득 전후 인벤토리<br/>수량 비교를 통해 성공 여부 판정
    UI-->>사용자: 아이템 획득 결과 및 가방 상태 출력
    deactivate UI