```mermaid
sequenceDiagram
    actor 사용자 as 플레이어
    participant UI as Create_Character_UI
    participant 전투
    participant 플레이어클래스 as 플레이어
    participant 전사
    participant 마법사
    participant 캐릭터

    사용자->>UI: 캐릭터생성(플레이어id, 캐릭터명, 직업, 레벨)
    UI->>전투: 캐릭터생성(플레이어id, 캐릭터명, 직업, 레벨)
    
    전투->>플레이어클래스: 플레이어체크(플레이어id)
    플레이어클래스-->>전투: boolean (true 반환 시 통과)
    
    alt 플레이어id == "hero"
        alt 직업 == "전사"
            전투->>전사: 객체 생성 및 능력치설정(HP=레벨x100, 공격력=레벨x15)
            전사->>캐릭터: 속성초기화(캐릭터명, 레벨, HP, 공격력)
            캐릭터-->>전사: void
            전사-->>전투: void
            전투->>전투: 현재캐릭터 변수에 전사 객체 저장
        else 직업 == "마법사"
            전투->>마법사: 객체 생성 및 능력치설정(HP=레벨x60, 공격력=레벨x25)
            마법사->>캐릭터: 속성초기화(캐릭터명, 레벨, HP, 공격력)
            캐릭터-->>마법사: void
            마법사-->>전투: void
            전투->>전투: 현재캐릭터 변수에 마법사 객체 저장
        end
        전투->>전투: 캐릭터생성성공 = true 상태 저장
    else 권한 없음
        전투->>전투: 에러 메시지 저장
    end
    
    전투-->>UI: void
    
    alt 생성 성공 시
        UI-->>사용자: Attack_Monster_UI.jsp 로 화면 즉시 이동
    else 생성 실패 시
        UI-->>사용자: 에러 메시지 화면 출력
    end